name: Build Extension

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:
    runs-on: macos-14
    strategy:
      matrix:
        version: [4.5]
    env:
      ADDON_NAME: molecularnodes
    steps:
      - uses: actions/checkout@v3
      - uses: SebRollen/toml-action@v1.2.0
        id: read_manifest
        with:
          file: '${{ env.ADDON_NAME }}/blender_manifest.toml'
          field: 'version'
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - uses: BradyAJohnston/setup-blender@v5
        with:
          version: ${{ matrix.version }}
      - name: Build extension
        run: |
          uv run build.py --blender-path $(which blender)
          mkdir artifact
          mv ${{ env.ADDON_NAME }}-${{ steps.read_manifest.outputs.value }}-*.zip ./artifact/.
      - name: Archive addon
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ADDON_NAME }}-build_${{ matrix.version }}-${{ github.sha }}
          retention-days: 3
          path: |
            ./artifact/*