---
title: "Materials"
jupyter: python3
fig-align: center
---

We can progrommatically access the properties of the materials like this:


```{python}
import molecularnodes as mn
cv = mn.Canvas(engine="EEVEE", resolution=(860, 540), transparent=True)
```


```{python}
mol = (
    mn.Molecule
    .fetch("8H1B")
    .add_style("surface", material = mn.material.AmbientOcclusion())
)
cv.frame_object(mol)
cv.snapshot()
```


```{python}
mol.styles[0].material.ambient_occlusion_distance = 0.1
cv.snapshot()
```
```{python}
mol.styles[0].material = mn.material.FlatOutline()
cv.snapshot()
```


```{python}
[x for x in dir(mn.material.AmbientOcclusion()) if not x.startswith("_")]
```