---
title: Styles
description: "Examples of using Styles"
jupyter: python3
execute:
  daemon: false
---

Different styles or representations can be added to molecules.

## Setup Molecular Nodes

```{python}
#| warning: false
import molecularnodes as mn
from molecularnodes.nodes import geometry as g
import MDAnalysis as mda
from MDAnalysis.tests.datafiles import DCD, PSF, TPR, XTC

u = mda.Universe(PSF, DCD)
canvas = mn.Canvas()
```


## Add a Molecule

A style can be specified when adding the molecule to Blender. Styles are given as a
string, with any style-specific options passed as keyword arguments to
[](`~mn.Molecule.add_style`).

```{python}
mol = mn.Molecule(u).add_style("ribbon", quality=4, peptide_radius=0.5)
canvas.frame_object(mol)
canvas.snapshot()
```

## Add Styles

Additional styles can be added to different selections of the universe using the
[](`~mn.Molecule.add_style`) API. Each call appends another style branch to the node tree.

```py
Molecule.add_style(
    style="spheres",
    selection=None,
    material=None,
    **kwargs,
)
```


- `style` param
  - A string, one of `ball_and_stick`, `cartoon`, `ribbon`, `spheres`, `sticks` or `surface`.
- `selection` param
  - An MDAnalysis selection phrase, an `AtomGroup`, or the name of an existing boolean
    attribute. A selection phrase is stored as a named attribute and used to mask the style.
- `material` param
  - A material name to append from the asset file, e.g. `MN Default`, `MN Flat Outline`,
    `MN Squishy`, `MN Transparent Outline` or `MN Ambient Occlusion`.
- `**kwargs`
  - Any remaining keyword arguments are passed to the style node, e.g. `geometry`,
    `quality`, `scale`, `peptide_radius`.

Add `StyleSpheres` to just residues 1 and 129. The string is used as a selection string to create an `mda.AtomGroup` which is used to create a boolean attribute, and that is used inside of the node tree for rendering.

```{python}
mol.add_style("spheres", geometry="Instance", selection="resid 1 129")
canvas.frame_object(mol)
canvas.snapshot()
```


```{python}
mol.add_style("surface", selection="resid 100:150", material="MN Transparent Outline")
canvas.frame_object(mol)
canvas.snapshot()
```

A selection can also be an `mda.AtomGroup`.

```{python}
mol.add_style("ball_and_stick", quality=4, selection=u.select_atoms("resid 180:200"))
canvas.frame_object(mol)
canvas.snapshot()
```

## Building a Style Tree

To take full control — or to start from a clean slate — build the node tree yourself with
the [](`~mn.Molecule.tree`) context manager. `tree.reset()` clears the existing tree and
yields the input `atoms` and output `join` sockets, so you can compose exactly the styles
you want with their specific parameters. This is the recommended way to create styling
that is more complex than simple styles and their selections. 

Attempting to expose the branching tree structure of Geometry Nodes via a Python API is 
a truly difficult task, and so instead we build the node tree using [](`nodebpy`) as a 
specialised scripting interface for node trees.

```{python}
mat = mn.nodes.material.append_material("MN Ambient Occlusion")

mol.dssp.init()
sel = mol.selections.from_string("resname LYS")

with mol.tree.reset() as (atoms, join):
  atoms >> g.StyleSticks(selection = sel.node(), scale=0.3, material=mat) >> join
  atoms >> g.SetColor(color=g.ColorRainbow()) >> g.StyleCartoon(material=mat) >> join
    
canvas.frame_object(mol)
canvas.snapshot()
```
